# Sanitized SQL — Group 12

This staging entry covers 19 SQL files reviewed and sanitized for eventual public-source use.

Direct student identifiers were removed from public output where practical. Schema fields used internally for joins, filtering, calculations, and aggregation remain when required by query logic. Do not commit query results, exports, screenshots, or other student-level data.

See `FERPA_AUDIT_GROUP_12.md` for the file-by-file privacy review. The complete sanitized package is retained as the Group 12 review artifact before final repository consolidation.

This review is privacy/PII-oriented and is not a legal determination of FERPA compliance.
